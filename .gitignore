# .NET
bin/
obj/
*.user
*.suo
.vs/

# Build output
artifacts/
publish/

# Local runtime data (never commit appdata / DB / segments / media)
/config/
/data/
*.db
*.db-wal
*.db-shm
/segments/
/media/

# Local runtime scratch (DB, segments, seeded creds) — never commit
**/_localdata/
*.import.json
*.seed.json

# Env / secrets
*.env
.env
appsettings.*.local.json
secrets.json

# Local deployment files (production compose, Unraid CA template) — kept out of the public repo
/deploy/

# Internal build diary (contains deployment specifics) — local only
DECISIONS.md

# Internal application audits — local only
/Codex/

# Local tooling config
.claude/
**/.claude/

# OS
.DS_Store
Thumbs.db
